// Route planning
// Have a route to GET payers and list their points
// Such as router.get('payer/name')
// Have a route to POST transactions that modify a payers points
// Such as router.post('payer/name/:points/add')
// router.post('payer/name/:points/subtract')


// Payer planning
// Payer will be a class
// Each instance of a class will have payer: string, points: integer, timestamp: date